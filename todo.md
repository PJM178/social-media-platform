- When liking posts, update the cache directly instead of refetching the queries - this should make the UI more responsive. --- DONE

- Do the above for new posts as well.

- Optimistic loading apollo client --- DONE